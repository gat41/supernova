A cosmology fitter using the Union 2.1 data (http://supernova.lbl.gov/Union/). 

#### Compilation

`$ ./compile aepow` will compile the code in the file `aepow.c` and return an executable object `aepow`.
